#include <malloc.h>

int * getSpecialProducts(int a[])
// Input: a vector of integers.
// Output: a vector array s
{
  int *result = (int *) malloc (sizeof(a));
  int n = sizeof(a)/sizeof(a[0])
    
    for (i=0; i< n; i++)
      {
	
      }

}
